name: Check and Deploy Docs
on:
  push:
    branches:
      - "master"

jobs:
  check_n_deploy_docs:
    name: Check and Deploy Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
      - name: Build Docs
        run: |
          source <(curl -s https://gist.githubusercontent.com/arHSM/54cd147deb0d5d84b9a4c3a2533aea6d/raw/d7b6ee0d075073747ebab2883e24ff8b5283de3f/deploy.sh) "${{ github.event.head_commit.message }}" ${{ secrets.GITHUB_TOKEN }} "${{ github.event.head_commit.committer.name }}" ${{ github.event.head_commit.id }}
